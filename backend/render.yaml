services:
  - type: web
    name: jharkhand-tourism-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server_integrated:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0